{
  "name": "dtslint-runner",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "run-clone": "node ./bin/index.js --clone --onlyTestTsNext --expectOnly",
    "test": "node ./node_modules/typescript/lib/tsc.js && node ./bin/index.js --clone --onlyTestTsNext --expectOnly",
    "lint": "tslint --project tsconfig.json --format stylish"
  },
  "license": "MIT",
  "devDependencies": {
    "tslint": "^5.12.0",
    "typescript": "next"
  },
  "dependencies": {
    "dtslint": "latest",
    "fs-extra": "^4.0.1",
    "stats-lite": "^2.2.0",
    "@types/fs-extra": "^4.0.0",
    "@types/node": "^10.14.12"
  }
}
